# Ubicación de las especificaciones

Los archivos `*.spec.md` viven en **`SDD/tile/specs/`** (dentro del tile) para que **`tessl tile publish ./SDD/tile`** los empaquete junto a `docs/` y `rules/`. Esta carpeta solo conserva esta nota para enlaces antiguos.
